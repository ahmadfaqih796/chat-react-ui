import { Box, TextField } from '@mui/material';
import React, { useState } from 'react';

const Register = () => {
  const [passwordVisible, setPasswordVisible] = useState(false);

  return (
    <Box>
      <TextField label="sasas" />
    </Box>
  );
};

export default Register;
